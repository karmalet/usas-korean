# ─────────────────────────────────────────────
# USAS Korean Hate Speech Tagger — 의존성 목록
# ─────────────────────────────────────────────
# 공통 필수
torch>=2.2,<3.0
transformers>=4.30.0

# 방법 1, 3 필수
wsd-torch-models

# 방법 2 필수
# spaCy 모델은 별도 pip install (아래 README 참고)
pymusas
spacy>=3.6.0

# 방법 3 필수 (+ Java 8 이상 별도 설치 필요)
konlpy

# 유틸리티
pandas          # 대용량 배치 처리 시 활용
tqdm            # 진행 표시줄 (선택)
